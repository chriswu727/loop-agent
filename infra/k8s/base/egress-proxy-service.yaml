apiVersion: v1
kind: Service
metadata:
  name: egress-proxy
spec:
  selector:
    app.kubernetes.io/name: egress-proxy
  ports:
    - name: proxy
      port: 8080
      targetPort: proxy
    - name: admin
      port: 8081
      targetPort: admin
